package com.piotrak.modularity.rules;

import com.piotrak.connectivity.ICommand;

public class Rules_Monitor implements IRules {
    
    @Override
    public void useRules(ICommand command) {
    
    }
}
